
{% liquid
  assign base_text = section.settings.text_color | default: '#111111'
  assign base_bg = section.settings.background_color | default: '#F7F4F0'
  assign page_width_px = settings.page_width | default: 1200
  assign horizontal_pad = section.settings.horizontal_padding | default: 24
  assign heading_size_preset = section.settings.heading_size_preset | default: 'default'
  case heading_size_preset
    when 'sm'
      assign header_size_mobile = '1.4'
      assign header_size_desktop = '2.0'
    when 'md'
      assign header_size_mobile = '1.6'
      assign header_size_desktop = '2.4'
    when 'lg'
      assign header_size_mobile = '2.0'
      assign header_size_desktop = '3.2'
    when 'xl'
      assign header_size_mobile = '2.4'
      assign header_size_desktop = '3.8'
    when 'xxl'
      assign header_size_mobile = '2.8'
      assign header_size_desktop = '4.4'
    else
      assign header_size_mobile = '1.75'
      assign header_size_desktop = '2.75'
  endcase
  assign header_weight = section.settings.header_font_weight | default: 600
  assign body_weight = section.settings.body_font_weight | default: 400
  assign body_size = section.settings.body_font_size | default: 16
  assign body_size_small = section.settings.body_font_size_small | default: 12
  assign header_line_height = section.settings.header_line_height | default: '1.05'
  assign header_letter_spacing = section.settings.header_letter_spacing | default: 0
  assign heading_color = section.settings.heading_color | default: base_text
  assign body_line_height = section.settings.body_line_height | default: '1.5'
  assign body_letter_spacing = section.settings.body_letter_spacing | default: 0
  assign section_pad_top = section.settings.section_padding_top | default: 40
  assign section_pad_bottom = section.settings.section_padding_bottom | default: 40
  assign accent_color = section.settings.accent_color | default: '#E36E15'
  assign secondary_text_color = section.settings.secondary_text_color | default: '#82906B'
  assign item_title_font = section.settings.item_title_font
%}
<section id="bfc-new-arrivals-{{ section.id }}" class="bfc-combined" aria-label="{{ section.settings.section_label | default: 'BFC New Arrivals' }}">
  <style>
    {{ item_title_font | font_face: font_display: 'swap' }}

    /* Fix shopify-section wrapper background & spacing */
    #shopify-section-{{ section.id }} {
      background-color: {{ base_bg }};
      margin: 0;
      padding: {{ section_pad_top }}px 0 {{ section_pad_bottom }}px;
      width: 100%;
    }
    #bfc-new-arrivals-{{ section.id }}{
      --bfc-header-family: var(--font-heading-family, inherit);
      --bfc-body-family: var(--font-body-family, inherit);
      --bfc-header-weight: {{ header_weight }};
      --bfc-body-weight: {{ body_weight }};
      --bfc-header-size-mobile: {{ header_size_mobile }}rem;
      --bfc-header-size-desktop: {{ header_size_desktop }}rem;
      --bfc-horizontal-pad: {{ horizontal_pad }}px;
      --bfc-page-width: {{ page_width_px }}px;
      --bfc-align-pad: clamp(
        var(--bfc-horizontal-pad),
        calc((100vw - min(100vw, var(--bfc-page-width))) / 2 + var(--bfc-horizontal-pad)),
        120px
      );
      --bfc-body-size: {{ body_size }}px;
      --bfc-body-size-small: {{ body_size_small }}px;
      --bfc-text: {{ base_text }};
      --bfc-bg: {{ base_bg }};
      --bfc-heading-color: {{ heading_color }};
      --bfc-header-letter-spacing: {{ header_letter_spacing }}px;
      --bfc-header-line-height: {{ header_line_height }};
      --bfc-body-letter-spacing: {{ body_letter_spacing }}px;
      --bfc-body-line-height: {{ body_line_height }};
      --bfc-accent: {{ accent_color }};
      --bfc-secondary-text: {{ secondary_text_color }};
      --bfc-item-title-family: {{ item_title_font.family | default: 'Poppins' }}, {{ item_title_font.fallback_families | default: 'sans-serif' }};
      --bfc-item-title-weight: {{ item_title_font.weight | default: 500 }};
      color: var(--bfc-text);
      background: var(--bfc-bg);
      font-family: var(--bfc-body-family);
      font-weight: var(--bfc-body-weight);
      font-size: var(--bfc-body-size);
    }
    #bfc-new-arrivals-{{ section.id }} .bfc-heading{
      font-family: var(--bfc-header-family, inherit);
      font-size: clamp(var(--bfc-header-size-mobile, 1.75rem), 3.5vw, var(--bfc-header-size-desktop, 2.75rem));
      font-weight: var(--bfc-header-weight, 300);
      line-height: var(--bfc-header-line-height, 1.05);
      letter-spacing: var(--bfc-header-letter-spacing, -0.02em);
      margin: 0;
      color: var(--bfc-heading-color, #111111);
    }
        #bfc-new-arrivals-{{ section.id }} .bfc-block{
      margin: 0 auto;
    }
    #bfc-new-arrivals-{{ section.id }} .bfc-block + .bfc-block{
      margin-top: 24px;
    }
    #bfc-new-arrivals-{{ section.id }} .visually-hidden{
      position: absolute;width: 1px;height: 1px;padding: 0;margin: -1px;overflow: hidden;clip: rect(0,0,0,0);white-space: nowrap;border: 0;
    }
    #bfc-new-arrivals-{{ section.id }} a:focus-visible,
    #bfc-new-arrivals-{{ section.id }} button:focus-visible,
    #bfc-new-arrivals-{{ section.id }} input:focus-visible{
      outline: 2px solid currentColor;
      outline-offset: 2px;
    }
    @media (prefers-reduced-motion: reduce){
      #bfc-new-arrivals-{{ section.id }} *{
        animation-duration: .01ms !important;
        animation-iteration-count: 1 !important;
        transition-duration: .01ms !important;
      }
    }
  
    #bfc-new-arrivals-{{ section.id }} .bfc-eyebrow{
      font-family: var(--bfc-body-family);
      font-size: var(--bfc-body-size-small);
      font-weight: 600;
      letter-spacing: 0.18em;
      text-transform: uppercase;
      color: var(--bfc-secondary-text);
      margin: 0 0 10px;
    }
    #bfc-new-arrivals-{{ section.id }} .bfc-item-title{
      font-family: var(--bfc-item-title-family);
      font-weight: var(--bfc-item-title-weight);
      font-size: var(--bfc-body-size);
      line-height: 1.25;
      letter-spacing: -0.01em;
      color: var(--bfc-text);
    }

    #bfc-new-arrivals-{{ section.id }} .bfc-detail-text{
      font-family: var(--bfc-body-family);
      font-size: var(--bfc-body-size-small);
      font-weight: var(--bfc-body-weight);
      letter-spacing: var(--bfc-body-letter-spacing);
      line-height: var(--bfc-body-line-height);
      color: var(--bfc-secondary-text);
      margin: 0;
    }

  </style>
  {% for block in section.blocks %}
    {% case block.type %}
      {% when 'new_arrivals' %}
        {% assign items_to_show = block.settings.items_to_show | default: 12 %}
        {% assign initial_items = block.settings.initial_items | default: 12 %}
        {% assign anchor_id = block.settings.anchor_id | strip | default: 'new-arrivals' %}
        {% assign manual_products = block.settings.manual_products | compact %}
        {% assign has_manual = false %}
        {% if manual_products and manual_products.size > 0 %}
          {% assign has_manual = true %}
        {% endif %}
        {% assign selected_collection = block.settings.collection %}
        {% assign has_single_collection = false %}
        {% if selected_collection %}
          {% assign has_single_collection = true %}
        {% endif %}
        {% assign use_newest_from_collection = block.settings.use_newest_from_collection %}
        {% assign full_rows_only = block.settings.full_rows_only %}
        {% assign full_rows_columns_desktop = block.settings.full_rows_columns_desktop | default: 5 %}
        {% assign disable_see_more = block.settings.disable_see_more %}
        {% assign products_to_render = nil %}
        {% assign source_type = '' %}
        {% if has_manual %}
          {% assign products_to_render = manual_products %}
          {% assign source_type = 'products' %}
        {% elsif has_single_collection %}
          {% if use_newest_from_collection %}
            {% assign products_to_render = selected_collection.products | sort: 'created_at' | reverse %}
          {% else %}
            {% assign products_to_render = selected_collection.products %}
          {% endif %}
          {% assign source_type = 'products' %}
        {% else %}
          {% assign product_handles_string = ',' %}
          {% assign product_counter = 0 %}
          {% assign arrivals_group = block.settings.group_id | default: block.id %}
          {% for source in section.blocks %}
            {% if product_counter >= items_to_show %}
              {% break %}
            {% endif %}
            {% if source.type == 'collection_source' and source.settings.group_id == arrivals_group %}
              {% if source.settings.collection %}
                {% assign collection_products = source.settings.collection.products %}
                {% for product in collection_products %}
                  {% if product_counter >= items_to_show %}
                    {% break %}
                  {% endif %}
                  {% assign token = ',' | append: product.handle | append: ',' %}
                  {% unless product_handles_string contains token %}
                    {% assign product_handles_string = product_handles_string | append: product.handle | append: ',' %}
                    {% assign product_counter = product_counter | plus: 1 %}
                  {% endunless %}
                {% endfor %}
              {% endif %}
            {% endif %}
          {% endfor %}
          {% assign products_to_render = product_handles_string | split: ',' | uniq | compact %}
          {% assign source_type = 'handles' %}
        {% endif %}
        {% assign render_limit = items_to_show %}
        {% if full_rows_only %}
          {% assign remainder = render_limit | modulo: full_rows_columns_desktop %}
          {% if remainder != 0 and render_limit > full_rows_columns_desktop %}
            {% assign render_limit = render_limit | minus: remainder %}
          {% endif %}
        {% endif %}
        {% if initial_items > render_limit %}
          {% assign initial_items = render_limit %}
        {% endif %}
        {% if disable_see_more %}
          {% assign initial_items = render_limit %}
        {% endif %}
        {% assign arrivals_radius = block.settings.product_radius %}
        {% if block.settings.square_corners %}
          {% assign arrivals_radius = 0 %}
        {% endif %}
        <section
          id="{{ anchor_id }}"
          class="bfc-block bfc-new-arrivals"
          aria-label="New arrivals"
          {{ block.shopify_attributes }}
        >
          <div class="new-arrivals-inner">
            {% if block.settings.heading != blank %}
              <h2 class="section-heading bfc-heading">{{ block.settings.heading | strip_html | escape }}</h2>
            {% endif %}
            {% if block.settings.text != blank %}
              <div class="section-text rte">
                {{ block.settings.text }}
              </div>
            {% endif %}
            {% assign total_products = products_to_render | size | default: 0 %}
            {% if products_to_render and total_products > 0 %}
              <div class="grid" id="{{ anchor_id }}-grid" role="list">
                {% assign product_index = 0 %}
                {% if source_type == 'products' %}
                  {% for product in products_to_render limit: render_limit %}
                    {% if product == blank %}
                      {% continue %}
                    {% endif %}
                    {% assign product_index = product_index | plus: 1 %}
                    {% assign image = product.featured_image %}
                    {% assign selected_variant = product.selected_or_first_available_variant %}
                    <div
                      class="product-card{% if product_index > initial_items %} product-card--hidden{% endif %}"
                      data-index="{{ product_index }}"
                      role="listitem"
                    >
                      <a href="{{ product.url }}" class="product-link" aria-label="{{ product.title | escape }}">
                        <div class="product-image-wrapper">
                          {% if image %}
                            <img
                              src="{{ image | image_url: width: 800 }}"
                              alt="{{ product.title | escape }}"
                              loading="lazy"
                              class="product-image"
                              width="{{ image.width }}"
                              height="{{ image.height }}"
                            >
                          {% else %}
                            <div class="placeholder" aria-hidden="true">No image available</div>
                          {% endif %}
                        </div>
                        {% if block.settings.info_display == 'below' %}
                          <div class="product-info below">
                            <p class="product-title">{{ product.title }}</p>
                            <p class="product-price">{{ product.price | money }}</p>
                          </div>
                        {% endif %}
                        {% if block.settings.info_display == 'hover' %}
                          <div class="product-info hover" aria-hidden="true">
                            <p class="product-title">{{ product.title }}</p>
                            <p class="product-price">{{ product.price | money }}</p>
                          </div>
                        {% endif %}
                      </a>
                      {% if selected_variant %}
                        <form method="post" action="{{ routes.cart_add_url }}" class="quick-add-form" aria-label="Add {{ product.title | escape }} to cart">
                          <input type="hidden" name="id" value="{{ selected_variant.id }}">
                          <input type="hidden" name="quantity" value="1">
                          <button type="submit" class="quick-add-button" {% unless product.available %}disabled aria-disabled="true"{% endunless %}>
                            <span aria-hidden="true">+</span>
                            <span class="visually-hidden">Add {{ product.title | escape }} to cart</span>
                          </button>
                        </form>
                      {% endif %}
                    </div>
                  {% endfor %}
                {% else %}
                  {% assign rendered_count = 0 %}
                  {% for handle in products_to_render %}
                    {% if rendered_count >= render_limit %}
                      {% break %}
                    {% endif %}
                    {% if handle == '' %}
                      {% continue %}
                    {% endif %}
                    {% assign product = all_products[handle] %}
                    {% if product %}
                      {% assign rendered_count = rendered_count | plus: 1 %}
                      {% assign product_index = product_index | plus: 1 %}
                      {% assign image = product.featured_image %}
                      {% assign selected_variant = product.selected_or_first_available_variant %}
                      <div
                        class="product-card{% if product_index > initial_items %} product-card--hidden{% endif %}"
                        data-index="{{ product_index }}"
                        role="listitem"
                      >
                        <a href="{{ product.url }}" class="product-link" aria-label="{{ product.title | escape }}">
                          <div class="product-image-wrapper">
                            {% if image %}
                              <img
                                src="{{ image | image_url: width: 800 }}"
                                alt="{{ product.title | escape }}"
                                loading="lazy"
                                class="product-image"
                                width="{{ image.width }}"
                                height="{{ image.height }}"
                              >
                            {% else %}
                              <div class="placeholder" aria-hidden="true">No image available</div>
                            {% endif %}
                          </div>
                          {% if block.settings.info_display == 'below' %}
                            <div class="product-info below">
                              <p class="product-title">{{ product.title }}</p>
                              <p class="product-price">{{ product.price | money }}</p>
                            </div>
                          {% endif %}
                          {% if block.settings.info_display == 'hover' %}
                            <div class="product-info hover" aria-hidden="true">
                              <p class="product-title">{{ product.title }}</p>
                              <p class="product-price">{{ product.price | money }}</p>
                            </div>
                          {% endif %}
                        </a>
                        {% if selected_variant %}
                          <form method="post" action="{{ routes.cart_add_url }}" class="quick-add-form" aria-label="Add {{ product.title | escape }} to cart">
                            <input type="hidden" name="id" value="{{ selected_variant.id }}">
                            <input type="hidden" name="quantity" value="1">
                            <button type="submit" class="quick-add-button" {% unless product.available %}disabled aria-disabled="true"{% endunless %}>
                              <span aria-hidden="true">+</span>
                              <span class="visually-hidden">Add {{ product.title | escape }} to cart</span>
                            </button>
                          </form>
                        {% endif %}
                      </div>
                    {% endif %}
                  {% endfor %}
                {% endif %}
              </div>
              {% if disable_see_more == false %}
                {% if product_index > initial_items or block.settings.show_back_to_top %}
                  <div class="show-more-wrapper">
                    <div class="show-more-controls">
                      {% if product_index > initial_items %}
                        <button type="button" class="show-more-button" data-show-more aria-expanded="false" aria-controls="{{ anchor_id }}-grid">
                          {{ block.settings.show_more_label | default: 'Display more' }}
                        </button>
                        <button type="button" class="collapse-button" data-collapse style="display: none;" aria-expanded="true" aria-controls="{{ anchor_id }}-grid">
                          {{ block.settings.collapse_label | default: 'Collapse' }}
                        </button>
                      {% endif %}
                    </div>
                    {% if block.settings.show_back_to_top %}
                      <button type="button" class="back-to-top-button" data-back-to-top>
                        {{ block.settings.back_to_top_label | default: 'Back to top' }}
                      </button>
                    {% endif %}
                  </div>
                {% endif %}
              {% endif %}
            {% endif %}
            {% if block.settings.button_text != blank and block.settings.button_link != blank %}
              <div class="cta-wrapper">
                <a href="{{ block.settings.button_link }}" class="button button--secondary">{{ block.settings.button_text }}</a>
              </div>
            {% endif %}
          </div>
          {% style %}
            #{{ anchor_id }}{
              --bg: {{ block.settings.background_color }};
              --text: {{ block.settings.text_color }};
              --radius: {{ arrivals_radius }}px;
              --bfc-block-pad: calc(var(--bfc-align-pad) + {{ block.settings.side_padding_vw }}vw);
              color: var(--text);
              background: var(--bg);
              padding: {{ block.settings.padding_top }}px var(--bfc-block-pad) {{ block.settings.padding_bottom }}px;
            }
            #{{ anchor_id }} .new-arrivals-inner {
              margin: 0 auto;
            }
            #{{ anchor_id }} .section-heading {
              margin: 0 0 18px;
              max-width: {{ block.settings.header_max_width | default: 820 }}px;
              width: 100%;
            }
            #{{ anchor_id }} .section-text {
              max-width: {{ block.settings.text_max_width | default: 820 }}px;
              margin: 0 auto 28px;
            }
            {% case block.settings.header_text_align %}
              {% when 'left' %}
                #{{ anchor_id }} .section-heading { text-align: left; margin-left: 0; margin-right: auto; }
              {% when 'right' %}
                #{{ anchor_id }} .section-heading { text-align: right; margin-left: auto; margin-right: 0; }
              {% else %}
                #{{ anchor_id }} .section-heading { text-align: center; margin-left: auto; margin-right: auto; }
            {% endcase %}
            {% case block.settings.text_align %}
              {% when 'left' %}
                #{{ anchor_id }} .section-text { text-align: left; margin-left: 0; margin-right: auto; }
              {% when 'right' %}
                #{{ anchor_id }} .section-text { text-align: right; margin-left: auto; margin-right: 0; }
              {% else %}
                #{{ anchor_id }} .section-text { text-align: center; margin-left: auto; margin-right: auto; }
            {% endcase %}
            #{{ anchor_id }} .grid {
              display: grid;
              gap: 16px;
              {% if full_rows_only %}
                grid-template-columns: repeat({{ full_rows_columns_desktop }}, minmax(0, 1fr));
              {% else %}
                grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
              {% endif %}
            }
            @media (max-width: 768px) {
              {% if block.settings.ignore_padding_mobile %}
                #{{ anchor_id }}{
                  --bfc-block-pad: var(--bfc-horizontal-pad);
                }
              {% endif %}
              #{{ anchor_id }} .grid {
                grid-template-columns: repeat(2, minmax(0, 1fr));
              }
            }
            #{{ anchor_id }} .product-card {
              position: relative;
              overflow: hidden;
              background: #fff;
              border-radius: var(--radius);
              transition: transform 0.2s ease;
            }
            #{{ anchor_id }} .product-card:hover { transform: translateY(-4px); }
            {% if disable_see_more %}
              #{{ anchor_id }} .product-card.product-card--hidden { display: block; }
            {% else %}
              #{{ anchor_id }} .product-card.product-card--hidden { display: none; }
            {% endif %}
            #{{ anchor_id }} .product-link {
              display: flex;
              flex-direction: column;
              height: 100%;
              text-decoration: none;
              color: inherit;
            }
            #{{ anchor_id }} .product-image-wrapper {
              position: relative;
              padding-top: 100%;
              background: #f9f9f9;
              overflow: hidden;
              border-top-left-radius: var(--radius);
              border-top-right-radius: var(--radius);
            }
            #{{ anchor_id }} .product-image {
              position: absolute;
              inset: 0;
              width: 100%;
              height: 100%;
              object-fit: contain;
            }
            #{{ anchor_id }} .placeholder {
              position: absolute;
              top: 50%;
              left: 50%;
              transform: translate(-50%, -50%);
              text-align: center;
              color: #666;
              font-size: var(--bfc-body-size-small);
            }
            #{{ anchor_id }} .product-info {
              padding: 12px;
              text-align: center;
              background: #fff;
              transition: opacity 0.2s ease, transform 0.2s ease;
              font-size: var(--bfc-body-size);
            }
            #{{ anchor_id }} .product-info.hover {
              position: absolute;
              left: 0;
              right: 0;
              bottom: 0;
              opacity: 0;
              transform: translateY(8px);
              background: rgba(255, 255, 255, 0.95);
            }
            #{{ anchor_id }} .product-card:hover .product-info.hover {
              opacity: 1;
              transform: translateY(0);
            }
            #{{ anchor_id }} .product-title {
              margin: 0 0 4px;
              font-size: var(--bfc-body-size);
              font-weight: var(--bfc-header-weight);
              letter-spacing: 0.02em;
            }
            #{{ anchor_id }} .product-price {
              margin: 0;
              font-size: var(--bfc-body-size-small);
              color: #444;
            }
            #{{ anchor_id }} .product-info.below { border-top: 0; }
            #{{ anchor_id }} .cta-wrapper { text-align: center; margin-top: 36px; }
            #{{ anchor_id }} .quick-add-form {
              position: absolute;
              bottom: 8px;
              right: 8px;
              z-index: 2;
            }
            #{{ anchor_id }} .quick-add-button {
              min-width: 24px;
              min-height: 24px;
              border-radius: 0;
              border: none;
              background: transparent;
              color: #111;
              font-size: 20px;
              line-height: 1;
              display: inline-flex;
              align-items: center;
              justify-content: center;
              cursor: pointer;
              padding: 0;
              box-shadow: none;
              transition: color 0.15s ease, transform 0.15s ease;
            }
            #{{ anchor_id }} .quick-add-button:focus-visible { outline: 1px solid currentColor; outline-offset: 2px; }
            #{{ anchor_id }} .quick-add-button:hover { color: #000; transform: translateY(-1px); }
            #{{ anchor_id }} .quick-add-button:disabled { cursor: not-allowed; opacity: 0.45; }
            #{{ anchor_id }} .visually-hidden {
              position: absolute;
              width: 1px;
              height: 1px;
              padding: 0;
              margin: -1px;
              overflow: hidden;
              clip: rect(0, 0, 0, 0);
              white-space: nowrap;
              border: 0;
            }
            #{{ anchor_id }} .show-more-wrapper {
              margin-top: 24px;
              display: flex;
              align-items: center;
              justify-content: space-between;
              gap: 12px;
              flex-wrap: wrap;
            }
            #{{ anchor_id }} .show-more-controls {
              display: flex;
              align-items: center;
              gap: 10px;
            }
            #{{ anchor_id }} .show-more-button,
            #{{ anchor_id }} .collapse-button,
            #{{ anchor_id }} .back-to-top-button {
              padding: 10px 32px;
              border-radius: var(--radius);
              border: 1px solid #111;
              background: #fff;
              font-size: var(--bfc-body-size-small);
              letter-spacing: 0.08em;
              text-transform: uppercase;
              cursor: pointer;
              transition: background-color 0.15s ease, color 0.15s ease;
            }
            #{{ anchor_id }} .show-more-button:hover,
            #{{ anchor_id }} .collapse-button:hover,
            #{{ anchor_id }} .back-to-top-button:hover {
              background: #111;
              color: #fff;
            }
            #{{ anchor_id }} .collapse-button,
            #{{ anchor_id }} .back-to-top-button {
              font-size: var(--bfc-body-size-small);
              padding: 8px 20px;
              opacity: 0.9;
            }
            @media (max-width: 600px) {
              #{{ anchor_id }} .show-more-wrapper { flex-direction: column; align-items: center; }
            }
          {% endstyle %}
          <script>
            (function() {
              var root = document.getElementById('{{ anchor_id }}');
              if (!root) return;
              var initialItems = {{ initial_items | json }};
              var disableSeeMore = {{ disable_see_more | json }};
              root.addEventListener('submit', function(event) {
                var form = event.target;
                if (!form.classList || !form.classList.contains('quick-add-form')) return;
                event.preventDefault();
                var button = form.querySelector('button');
                if (button) {
                  button.disabled = true;
                  button.setAttribute('aria-busy', 'true');
                }
                var formData = new FormData(form);
                if (!formData.get('quantity')) {
                  formData.append('quantity', '1');
                }
                var body = new URLSearchParams(formData).toString();
                fetch('{{ routes.cart_add_url }}.js', {
                  method: 'POST',
                  headers: {
                    'Content-Type': 'application/x-www-form-urlencoded; charset=UTF-8',
                    'X-Requested-With': 'XMLHttpRequest'
                  },
                  body: body,
                  credentials: 'same-origin'
                })
                  .then(function(res) {
                    if (!res.ok) throw new Error('Add to cart failed');
                    return res.json();
                  })
                  .then(function() {
                    return fetch('/cart/update.js', {
                      method: 'POST',
                      headers: {
                        'Content-Type': 'application/json',
                        'X-Requested-With': 'XMLHttpRequest'
                      },
                      body: JSON.stringify({
                        updates: {},
                        sections: ['cart-drawer', 'cart-icon-bubble'],
                        sections_url: window.location.pathname
                      }),
                      credentials: 'same-origin'
                    });
                  })
                  .then(function(res) {
                    if (!res.ok) throw new Error('Cart update fetch failed');
                    return res.json();
                  })
                  .then(function(state) {
                    var drawer = document.querySelector('cart-drawer');
                    if (drawer && typeof drawer.renderContents === 'function') drawer.renderContents(state);
                    if (drawer && typeof drawer.open === 'function') drawer.open();
                  })
                  .catch(function(error) {
                    console.error('Quick add failed', error);
                  })
                  .finally(function() {
                    if (button) {
                      button.disabled = false;
                      button.removeAttribute('aria-busy');
                    }
                  });
              });
              if (disableSeeMore) return;
              var showMoreBtn = root.querySelector('[data-show-more]');
              var collapseBtn = root.querySelector('[data-collapse]');
              if (showMoreBtn && collapseBtn) {
                showMoreBtn.addEventListener('click', function() {
                  var cards = root.querySelectorAll('.product-card');
                  for (var i = 0; i < cards.length; i++) {
                    var idx = parseInt(cards[i].getAttribute('data-index'), 10) || 0;
                    if (idx > initialItems) {
                      cards[i].classList.remove('product-card--hidden');
                      cards[i].style.display = '';
                    }
                  }
                  showMoreBtn.setAttribute('aria-expanded', 'true');
                  showMoreBtn.style.display = 'none';
                  collapseBtn.setAttribute('aria-expanded', 'true');
                  collapseBtn.style.display = 'inline-block';
                });
                collapseBtn.addEventListener('click', function() {
                  var cards = root.querySelectorAll('.product-card');
                  for (var i = 0; i < cards.length; i++) {
                    var idx = parseInt(cards[i].getAttribute('data-index'), 10) || 0;
                    if (idx > initialItems) {
                      cards[i].classList.add('product-card--hidden');
                      cards[i].style.display = 'none';
                    } else {
                      cards[i].classList.remove('product-card--hidden');
                      cards[i].style.display = '';
                    }
                  }
                  collapseBtn.setAttribute('aria-expanded', 'false');
                  collapseBtn.style.display = 'none';
                  showMoreBtn.setAttribute('aria-expanded', 'false');
                  showMoreBtn.style.display = 'inline-block';
                  root.scrollIntoView({ behavior: 'smooth', block: 'start' });
                });
              }
              var backToTopBtn = root.querySelector('[data-back-to-top]');
              if (backToTopBtn) {
                backToTopBtn.addEventListener('click', function() {
                  window.scrollTo({ top: 0, behavior: 'smooth' });
                });
              }
            })();
          </script>
        </section>
    {% endcase %}
  {% endfor %}
</section>
{% schema %}
{
  "name": "BFC New Arrivals",
  "tag": "section",
  "settings": [
    {
      "type": "range",
      "id": "horizontal_padding",
      "label": "Horizontal padding",
      "min": 12,
      "max": 60,
      "step": 2,
      "unit": "px",
      "default": 24
    },
    {
      "type": "select",
      "id": "header_font_weight",
      "label": "Header font weight",
      "options": [
        {
          "value": "300",
          "label": "Light"
        },
        {
          "value": "400",
          "label": "Normal"
        },
        {
          "value": "500",
          "label": "Medium"
        },
        {
          "value": "600",
          "label": "SemiBold"
        },
        {
          "value": "700",
          "label": "Bold"
        }
      ],
      "default": "300"
    },
    {
      "type": "select",
      "id": "body_font_weight",
      "label": "Body font weight",
      "options": [
        {
          "value": "400",
          "label": "Normal"
        },
        {
          "value": "500",
          "label": "Medium"
        }
      ],
      "default": "400"
    },
    {
      "type": "range",
      "id": "body_font_size",
      "label": "Body font size",
      "min": 12,
      "max": 20,
      "step": 1,
      "unit": "px",
      "default": 16
    },
    {
      "type": "range",
      "id": "body_font_size_small",
      "label": "Small text size",
      "min": 10,
      "max": 16,
      "step": 1,
      "unit": "px",
      "default": 12
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Base text color",
      "default": "#111111"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Base background color",
      "default": "#F7F4F0"
    },
    {
      "type": "header",
      "content": "Heading Typography"
    },
    {
      "type": "select",
      "id": "heading_size_preset",
      "label": "Heading size",
      "info": "Controls heading size at all screen widths consistently.",
      "options": [
        {
          "value": "sm",
          "label": "Small"
        },
        {
          "value": "md",
          "label": "Medium"
        },
        {
          "value": "default",
          "label": "Default"
        },
        {
          "value": "lg",
          "label": "Large"
        },
        {
          "value": "xl",
          "label": "X-Large"
        },
        {
          "value": "xxl",
          "label": "XX-Large"
        }
      ],
      "default": "default"
    },
    {
      "type": "select",
      "id": "header_line_height",
      "label": "Heading line height",
      "options": [
        {
          "value": "1.0",
          "label": "1.0 (Tightest)"
        },
        {
          "value": "1.05",
          "label": "1.05 (Default)"
        },
        {
          "value": "1.1",
          "label": "1.1"
        },
        {
          "value": "1.2",
          "label": "1.2"
        },
        {
          "value": "1.3",
          "label": "1.3"
        },
        {
          "value": "1.4",
          "label": "1.4 (Loose)"
        }
      ],
      "default": "1.05"
    },
    {
      "type": "range",
      "id": "header_letter_spacing",
      "label": "Heading letter spacing",
      "min": -2,
      "max": 8,
      "step": 1,
      "unit": "px",
      "default": -2
    },
    {
      "type": "color",
      "id": "heading_color",
      "label": "Heading color",
      "info": "Falls back to base text color if unset",
      "default": "#111111"
    },
    {
      "type": "header",
      "content": "Body Typography"
    },
    {
      "type": "select",
      "id": "body_line_height",
      "label": "Body line height",
      "options": [
        {
          "value": "1.3",
          "label": "1.3 (Tight)"
        },
        {
          "value": "1.4",
          "label": "1.4"
        },
        {
          "value": "1.5",
          "label": "1.5 (Default)"
        },
        {
          "value": "1.6",
          "label": "1.6"
        },
        {
          "value": "1.8",
          "label": "1.8 (Loose)"
        }
      ],
      "default": "1.5"
    },
    {
      "type": "range",
      "id": "body_letter_spacing",
      "label": "Body letter spacing",
      "min": -1,
      "max": 4,
      "step": 1,
      "unit": "px",
      "default": 0
    },
    {
      "type": "header",
      "content": "Section Spacing"
    },
    {
      "type": "range",
      "id": "section_padding_top",
      "label": "Section top padding",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "default": 40
    },
    {
      "type": "range",
      "id": "section_padding_bottom",
      "label": "Section bottom padding",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "default": 40
    },
    {
      "type": "header",
      "content": "Section Colors"
    },
    {
      "type": "color",
      "id": "accent_color",
      "label": "Accent color",
      "info": "Used for links and highlights",
      "default": "#E36E15"
    },
    {
      "type": "color",
      "id": "secondary_text_color",
      "label": "Secondary text color",
      "info": "Used for eyebrows and small detail text",
      "default": "#82906B"
    },
    {
      "type": "font_picker",
      "id": "item_title_font",
      "label": "Item title font",
      "info": "Font used for product/item titles (e.g. the old best-seller title font)",
      "default": "poppins_n5"
    }
  ],
  "blocks": [
    {
      "type": "new_arrivals",
      "name": "New Arrivals Grid",
      "settings": [
        {
          "type": "text",
          "id": "group_id",
          "label": "Group ID (match collection sources)",
          "default": "new-arrivals-1"
        },
        {
          "type": "text",
          "id": "anchor_id",
          "label": "Custom anchor ID (optional)",
          "default": "new-arrivals"
        },
        {
          "type": "text",
          "id": "heading",
          "label": "Heading",
          "info": "Plain text only. Leave blank to hide.",
          "default": "New arrivals"
        },
        {
          "type": "richtext",
          "id": "text",
          "label": "Text",
          "default": "<p></p>"
        },
        {
          "type": "select",
          "id": "header_text_align",
          "label": "Header alignment",
          "default": "center",
          "options": [
            {
              "value": "left",
              "label": "Left"
            },
            {
              "value": "center",
              "label": "Center"
            },
            {
              "value": "right",
              "label": "Right"
            }
          ]
        },
        {
          "type": "select",
          "id": "text_align",
          "label": "Text alignment",
          "default": "center",
          "options": [
            {
              "value": "left",
              "label": "Left"
            },
            {
              "value": "center",
              "label": "Center"
            },
            {
              "value": "right",
              "label": "Right"
            }
          ]
        },
        {
          "type": "range",
          "id": "text_max_width",
          "label": "Text max width",
          "default": 820,
          "min": 420,
          "max": 1200,
          "step": 20,
          "unit": "px"
        },
        {
          "type": "range",
          "id": "header_max_width",
          "label": "Header max width",
          "default": 820,
          "min": 420,
          "max": 1200,
          "step": 20,
          "unit": "px"
        },
        {
          "type": "product_list",
          "id": "manual_products",
          "label": "Manual product selection"
        },
        {
          "type": "collection",
          "id": "collection",
          "label": "Collection (optional)"
        },
        {
          "type": "checkbox",
          "id": "use_newest_from_collection",
          "label": "Use newest products from chosen collection",
          "default": false
        },
        {
          "type": "checkbox",
          "id": "full_rows_only",
          "label": "Only show full-width rows (desktop)",
          "default": false
        },
        {
          "type": "range",
          "id": "full_rows_columns_desktop",
          "label": "Desktop columns (for full-width rows)",
          "default": 5,
          "min": 2,
          "max": 6,
          "step": 1
        },
        {
          "type": "checkbox",
          "id": "disable_see_more",
          "label": "Hide \u201cDisplay more\u201d / \u201cCollapse\u201d (always)",
          "default": false
        },
        {
          "type": "range",
          "id": "items_to_show",
          "label": "Maximum items to show",
          "default": 12,
          "min": 4,
          "max": 48,
          "step": 1
        },
        {
          "type": "range",
          "id": "initial_items",
          "label": "Items visible before \u201cDisplay more\u201d",
          "default": 12,
          "min": 4,
          "max": 48,
          "step": 1
        },
        {
          "type": "text",
          "id": "show_more_label",
          "label": "\u201cDisplay more\u201d button label",
          "default": "Display more"
        },
        {
          "type": "text",
          "id": "collapse_label",
          "label": "Collapse button label",
          "default": "Collapse"
        },
        {
          "type": "checkbox",
          "id": "show_back_to_top",
          "label": "Show \u201cBack to top\u201d button",
          "default": true
        },
        {
          "type": "text",
          "id": "back_to_top_label",
          "label": "\u201cBack to top\u201d label",
          "default": "Back to top"
        },
        {
          "type": "select",
          "id": "info_display",
          "label": "Product info placement",
          "default": "hover",
          "options": [
            {
              "value": "hover",
              "label": "Show product info on hover"
            },
            {
              "value": "below",
              "label": "Show product info below image"
            }
          ]
        },
        {
          "type": "color",
          "id": "background_color",
          "label": "Background color",
          "default": "#FFFFFF"
        },
        {
          "type": "color",
          "id": "text_color",
          "label": "Text color",
          "default": "#1C1C1C"
        },
        {
          "type": "range",
          "id": "padding_top",
          "label": "Top padding",
          "default": 40,
          "min": 0,
          "max": 200,
          "step": 4,
          "unit": "px"
        },
        {
          "type": "range",
          "id": "padding_bottom",
          "label": "Bottom padding",
          "default": 40,
          "min": 0,
          "max": 200,
          "step": 4,
          "unit": "px"
        },
        {
          "type": "range",
          "id": "side_padding_vw",
          "label": "Side padding add-on (vw)",
          "min": 0,
          "max": 10,
          "step": 0.5,
          "unit": "vw",
          "default": 0
        },
        {
          "type": "checkbox",
          "id": "ignore_padding_mobile",
          "label": "Ignore side padding on mobile",
          "default": false
        },
        {
          "type": "range",
          "id": "product_radius",
          "label": "Product corner radius (px)",
          "default": 8,
          "min": 0,
          "max": 32,
          "step": 2
        },
        {
          "type": "checkbox",
          "id": "square_corners",
          "label": "Square corners",
          "default": false
        },
        {
          "type": "text",
          "id": "button_text",
          "label": "Bottom button text",
          "default": "SEE ALL"
        },
        {
          "type": "url",
          "id": "button_link",
          "label": "Bottom button link"
        }
      ]
    },
    {
      "type": "collection_source",
      "name": "Collection Source",
      "settings": [
        {
          "type": "text",
          "id": "group_id",
          "label": "Group ID (matches New Arrivals Grid)",
          "default": "new-arrivals-1"
        },
        {
          "type": "collection",
          "id": "collection",
          "label": "Collection"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "BFC New Arrivals"
    }
  ]
}
{% endschema %}
